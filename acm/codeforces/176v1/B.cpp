#include <iostream>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <algorithm>

using namespace std;

const int MAX = 1000000 + 50; 
int n;
int v[MAX];

int main() {
    cin >> n;
    for (i = 1; i <= n; i++) {
        v[i] = i;
    }
    for (i = 2; i <= n / 2; i++) {

    }
    return 0;
}
